#include <iostream>
#include <algorithm>
#include "MergeSortedKList.h"

using namespace std;
int main()
{
	Solution s;
	struct ListNode *l, *m, *n;
	vector<ListNode*> l2 = {l, m, n};

	s.mergeKLists(l2);
}
